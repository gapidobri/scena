generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model Video {
  id String @id @default(uuid())

  title        String
  url          String
  thumbnailUrl String?
}
